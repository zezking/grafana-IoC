apiVersion: 1

apps:
  - type: 'enze-grafanaioc-app'
    org_id: 1
    org_name: 'Enze'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
